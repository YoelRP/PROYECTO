En idle lo que se a se hace es verificar si se tienen uno de dos syncronizadores , uno para velocidad completa o baja  y otro para super alta velocidad , este pasar a "receive" si obtiene alguno de los dos , en se verifica el PID que debe pertenecer a un token si es valido se pasa a "state", en "STATE" se  verifica si la direccion es correcta y  hacia que endpoint se dirige, pregunta si este enpoint esta habilitado de no ser el caso pasa a stall , en caso de que sea en una entrada pasa sa data_in y una peticion de dato pasaria a data_out , o la que mas nos interesa para el caso del debuger que sea un dato de control pasa al estado setup_token 
